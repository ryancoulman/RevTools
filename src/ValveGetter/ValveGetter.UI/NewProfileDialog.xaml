<Window x:Class="ValveGetter.UI.NewProfileDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add New Profile" Height="180" Width="320"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>

        <!-- Profile Name -->
        <StackPanel Orientation="Vertical" Margin="0,0,0,10">
            <TextBlock Text="Profile Name:" Margin="0,0,0,5"/>
            <TextBox x:Name="ProfileNameTextBox" Width="250"/>
        </StackPanel>

        <!-- Use current settings -->
        <DockPanel Grid.Row="1" Margin="0,0,0,10">
            <RadioButton x:Name="UseCurrentSettingsRadio"
             GroupName="ProfileOption"
             Content="Use current settings"
             IsChecked="True"
             Margin="0,0,10,0"/>
            <RadioButton x:Name="UseDefaultSettingsRadio"
             GroupName="ProfileOption"
             Content="Restore defaults"
             IsChecked="False"
             Margin="10,0,0,0"/>
        </DockPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,0">
            <Button Content="OK" Width="80" Margin="0,0,10,0" Click="Ok_Click"/>
            <Button Content="Cancel" Width="80" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
